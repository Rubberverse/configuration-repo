# Rename to Homarr.pod and put it in ~/.config/containers/systemd/Homarr then reload systemctl daemon with systemctl --user daemon-reload
[Pod]
Network=pasta:--ipv4-only
# HTTP
PublishPort=127.0.0.1:9003:3000
# Websocket
PublishPort=127.0.0.1:9004:3001
# Name lookup failure fix
AddHost=systemd-homarr:0.0.0.0
AddHost=<your domain>:10.30.1.10
