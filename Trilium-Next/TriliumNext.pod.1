# Rename to TriliumNext.pod and put it in ~/.config/containers/systemd/TriliumNext then reload systemctl daemon with systemctl --user daemon-reload
[Pod]
Network=pasta:--ipv4-only
PublishPort=127.0.0.1:9105:8080
